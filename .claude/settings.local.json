{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "Bash(docker --version)",
      "PowerShell(docker run -p 8080:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:25.0 start-dev)",
      "PowerShell(docker stop da28fd9dbb78)"
    ]
  }
}
